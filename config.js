/**
 * The one line you have to change.
 *
 * Put your Worker's address here, exactly as Cloudflare printed it when you ran
 * `npx wrangler deploy`, with no trailing slash. It will look like:
 *
 *   https://whereabouts.something.workers.dev
 */
window.WA_CONFIG = {
  API_BASE: 'https://whereabouts.oakwicklogin.workers.dev',
  APP_VERSION: '1.4.0',
};
